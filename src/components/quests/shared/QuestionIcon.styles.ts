import { QuestionCircleOutlined } from "@ant-design/icons";
import styled from "styled-components";

// Reusable styled icon for form field help indicators
export const QuestionIcon = styled(QuestionCircleOutlined)`
  margin: 0 4px;
`;
